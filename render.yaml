services:
  - type: web
    name: sportzone-backend
    env: python
    buildCommand: "pip install -r requirements.txt && python fix_and_run.py"
    startCommand: "gunicorn app:app"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: JWT_SECRET_KEY
        generateValue: true